name 'libksba'
org 'amylum'

licenses 'COPYING.GPLv3'

deps(
  'libgpg-error': '1.26-1'
)

configure_flags(
  prefix: '/usr',
  'enable-maintainer-mode': nil
)
remove_linker_archives

cflags
harden

build do
  run './autogen.sh'
  configure
  make
  install
end

test do
  # TODO: add tests
end
